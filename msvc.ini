[binaries]
c = 'cl'

# MSVC doesn't have an equivalent of -march=native so each flag must be added
# manually, either to this file or using the command line.
[built-in options]
c_args = ['/MP', '/O2', '/GL', '/Gw', '/Qpar']
c_link_args = ['/LTCG']
